{
  "name": "kitchen-pos-monorepo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix dashboard run dev",
    "dev:dashboard": "npm --prefix dashboard run dev",
    "dev:portal": "npm --prefix portal run dev",
    "dev:kds": "npm --prefix kds run dev",
    "build:dashboard": "npm --prefix dashboard run build",
    "build:portal": "npm --prefix portal run build",
    "build:kds": "npm --prefix kds run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1",
    "lucide-react": "^0.479.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.2.0"
  }
}
